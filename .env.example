# 本地Devnet的RPC URL，默认端口5050
DEVNET_RPC_URL=http://localhost:5050
# Devnet账户的profile名称，需通过sncast account import设置
DEVNET_ACCOUNT_PROFILE=devnet
# Sepolia网络的RPC URL，需替换为实际URL（如Infura提供的）
SEPOLIA_RPC_URL=https://sepolia.starknet.io
# Sepolia账户的profile名称，需通过sncast account create和deploy设置
SEPOLIA_ACCOUNT_PROFILE=sepolia